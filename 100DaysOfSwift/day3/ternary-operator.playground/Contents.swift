import Cocoa

let firstCard = 10
let secondCard = 11

print(firstCard == secondCard ? "Both cards are equal" : "Cards are different")

if firstCard == secondCard {
    print ("Both cards are equal")
}else {
    print ("Cads are different")
}
